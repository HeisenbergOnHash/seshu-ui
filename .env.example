# Production backend (include /api or just the host — /api is appended automatically)
VITE_API_URL=https://seshu-backend.onrender.com/api

# Local development (optional — leave unset to use Vite proxy at /api)
# VITE_API_URL=http://localhost:8080/api
# VITE_API_PROXY_TARGET=http://localhost:8080
